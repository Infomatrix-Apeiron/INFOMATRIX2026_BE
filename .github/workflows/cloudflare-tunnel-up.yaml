name: cloudflare-tunnel-up

on: workflow_dispatch

# environments
# "PRODUCTION" | "PRERELEASE" | "DEVELOP"

env:
  HOST_USER: "ubuntu"
  APP_DIR: "/home/ubuntu/fin-tracker/be"
  COMPOSE_NAME: "cloudflare-tunnel-compose.yaml"
  PROJECT_NAME: "cloudflare_tunnel_tracker"

# secrets
# - REMOTE_HOST
# - SSH_KEY
# - SSH_PASSWORD
# - CLOUDFLARE_TUNNEL_TOKEN

jobs:
  deploy:
    environment: PRODUCTION
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Copy Docker Compose to Server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.REMOTE_HOST }}
          username: ${{ env.HOST_USER }}
          key: ${{ secrets.SSH_KEY }}
          source: ".infra/${{ env.COMPOSE_NAME }}"
          target: ${{ env.APP_DIR }}
          strip_components: 1
      
      - name: Run Tunnel
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.REMOTE_HOST }}
          username: ${{ env.HOST_USER }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            cd ${{ env.APP_DIR }}

            export CLOUDFLARE_TUNNEL_TOKEN="${{ secrets.CLOUDFLARE_TUNNEL_TOKEN }}"
  
            docker compose -p ${{ env.PROJECT_NAME }} -f ${{ env.COMPOSE_NAME }} down --remove-orphans || true
            docker compose -p ${{ env.PROJECT_NAME }} -f ${{ env.COMPOSE_NAME }} --env-file .env up -d
